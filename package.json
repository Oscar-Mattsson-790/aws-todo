{
  "name": "aws-todo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "zip aws-todo.zip index.js functions/** responses/** services/** && aws lambda update-function-code --function-name todoHandler-api --zip-file fileb://aws-todo.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
